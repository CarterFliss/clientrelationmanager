<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">
  
  <!-- enable use-expressions -->
  <http auto-config="true" use-expressions="true">
    <intercept-url pattern="/viewclients/**" access="hasAnyRole('ADMIN','MANAGER','USER')" />
    <intercept-url pattern="/viewclients/{id}" access="hasAnyRole('ADMIN','MANAGER','USER')" />
    <intercept-url pattern="/vieweventlog/" access="hasAnyRole('ADMIN','MANAGER','USER')" />
    <intercept-url pattern="/vieweventlog/addevent" access="hasAnyRole('ADMIN','MANAGER','USER')" />
    <intercept-url pattern="/viewevent/updatevent/{id}" access="hasAnyRole('ADMIN','MANAGER','USER')" />
    <intercept-url pattern="/viewclients/addclient" access="hasAnyRole('ADMIN','MANAGER')" />
    <intercept-url pattern="/viewclients/removeclient/{id}" access="hasAnyRole('ADMIN','MANAGER')" />
    <intercept-url pattern="/viewclients/updateclient/{id}" access="hasAnyRole('ADMIN','MANAGER')" />
    <intercept-url pattern="/vieweventlog/removeevent/{id}" access="hasRole('ADMIN')" />
    <intercept-url pattern="/viewusers/**" access="hasRole('ADMIN')" />
		
    <!-- access denied page -->
    <access-denied-handler error-page="/403" />
    
    <form-login 
      login-page="/login" 
      default-target-url="/" 
      always-use-default-target="true"
      authentication-failure-url="/login?error" 
      username-parameter="username"
      login-processing-url="/j_spring_security_login"
      password-parameter="password" />
    
    <logout logout-success-url="/login?logout" logout-url="/j_spring_security_logout"  />
    
    <csrf/>
  </http>
	
  <!-- select users and user_roles from database -->
  <authentication-manager>
    <authentication-provider>
      <password-encoder hash="sha" />
      <jdbc-user-service data-source-ref="ds"
          users-by-username-query="select Username,Password, User_Status from users where username=?"
          authorities-by-username-query="select Username, UserRole from roles where username =?  " />
    </authentication-provider>
  </authentication-manager>
</beans:beans>